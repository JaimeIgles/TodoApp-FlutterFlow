{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Task",
  "description": "305Soft schema for Firestore task documents in the Todo app.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "title": {
      "description": "Short title of the task.",
      "type": "string",
      "minLength": 1
    },
    "details": {
      "description": "Optional longer description of the task.",
      "type": ["string", "null"]
    },
    "completed": {
      "description": "Whether the task is completed.",
      "type": "boolean"
    },
    "created": {
      "description": "Timestamp when the task was created (ISO 8601).",
      "type": "string",
      "format": "date-time"
    },
    "user": {
      "description": "Reference to the user document who owns this task (Firestore reference path).",
      "type": "string",
      "pattern": "^/users/[A-Za-z0-9_-]+$"
    }
  },
  "required": ["title", "completed", "created", "user"]
}
